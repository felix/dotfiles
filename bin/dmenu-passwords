#!/bin/sh

MENU=dmenu
[ -n "$WAYLAND_DISPLAY" ] && MENU=bemenu
store=${PASSWORD_STORE_DIR-$HOME/.password-store}
list=$(find "$store" -name '*.gpg' |sed -r "s@${store}/(.+)\.gpg@\1@")
password=$(echo "$list" |$MENU -l 10 -p Password: "$@") || exit
pass show -c "$password" 2>/dev/null
