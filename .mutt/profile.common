unset preconnect imap_user imap_pass imap_authenticators
unset folder
unset from trash spoolfile record postponed
unset signature
unset crypt_autosign pgp_sign_as
unmy_hdr Organization
unmy_hdr X-PGP-Key
color status green black

# vim: set ft=muttrc:
